rules_version = '2';

// All data access goes through the authenticated Cloud Function (Admin SDK),
// which bypasses these rules. Direct client access to Firestore is denied.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
